# Dependencies
# This directory contains all of the modules installed by npm.
# It's huge and can be completely regenerated by running `npm install`.
/node_modules

# Environment Variables
# This file contains secrets like database passwords and API keys.
# It should NEVER be committed to the repository for security reasons.
# Instead, create a .env.example file with placeholder values.


# Logs
# Log files are generated during runtime and should not be versioned.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Build output / Compiled files
/dist
/build

# IDE / Editor configuration files
# These are specific to a user's machine and editor setup.
.vscode/
.idea/

# OS-generated files
.DS_Store
Thumbs.db